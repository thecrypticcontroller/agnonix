/// <reference types="vite/client" />
// SceneManager.tsx — DEPRECATED
// All orchestration has moved to App.tsx.
// This file is kept to avoid a dead-import compile error.
// It is no longer rendered by anything.

export function SceneManager() {
  return null;
}
