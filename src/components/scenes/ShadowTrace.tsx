// ShadowTrace.tsx — replaced by Universe.tsx in the cinematic rebuild
// Kept as an empty export to avoid stale import errors during transition.
export function ShadowTrace() { return null; }
export default ShadowTrace;
